# WP_BASE_URL is auto-discovered from the running wp-env (its assigned port
# can differ per machine/run). Only uncomment to force a specific URL.
# WP_BASE_URL=http://localhost:8888
WP_USERNAME=admin
WP_PASSWORD=password
